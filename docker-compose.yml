services:
  postgres:
    image: postgres:16-alpine
    container_name: cth_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ct_hub
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: